---
// Define the array of projects directly in the frontmatter script.
const otherprojects = [
  {
    url: "https://www.lexingtonthemes.com/",
    title: "Remote Meetup",
    description: "When great speakers meet amazing audience.",
  },
  {
    url: "https://www.oxbowui.com/",
    title: "Edutainment",
    description: "Sharing knowledge through our platforms.",
  },
  {
    url: "https://www.colorsandfonts.com/",
    title: "Remote Talk",
    description:
      "When you need some guidances and perspective from professionals in their fields.",
  },
  {
    url: "https://www.svgdoodles.com/",
    title: "We Work",
    description:
      "A place (or maybe a moment) to work together, every WEdnesday.",
  },
  // Add more projects as needed...
];

// Function to calculate `data-aos-duration` based on index
function calculateAosDuration(index) {
  return 1500 + index * 500; // Example: Increment duration by 500ms for each project
}
---

<section class="relative">
  <div class="px-8 2xl:max-w-7xl mx-auto py-32">
    <div class="space-y-2">
      <!-- Section Title and Decorative Elements -->
      <div class="h-0.5 bg-secondary"></div>
      <h3 class="text-secondary text-base font-medium">
        <span>Main</span>
        <span class="font-bold">events</span>
      </h3>
    </div>

    <ul
      class="mx-auto grid grid-cols-1 sm:grid-cols-2 text-secondary md:grid-cols-3 lg:grid-cols-4 items-start gap-6 mt-32"
      role="list"
    >
      {
        otherprojects.map((project, index) => (
          <li class="flow-root h-full">
            <a href={project.url}>
              <div class="h-full flex flex-col justify-between">
                <div>
                  <p class="group-hover:text-secondary/70 text-secondary text-9xl font-black lg:rotate-90 xl:-translate-x-28 lg:-translate-x-16 lg:mb-8 xl:mb-0  lg:pb-8">
                    0{index + 1}.
                  </p>
                  <p class="italic shrink-0">{project.title}</p>
                  <p class="text-base mt-3 leading-relaxed">
                    {project.description}
                  </p>
                </div>
                <div>
                  <p class="mt-3 text-base font-semibold">
                    Learn more &nbsp; ↗
                  </p>
                </div>
              </div>
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</section>
